{
  "schema_version": "1",
  "presets": [
    {
      "id": "sftpgo-sync",
      "name": "SFTPGo Sync",
      "description": "Reads JustIAM group app mappings as the source of truth and keeps SFTPGo groups, users, and group memberships in sync — emulating OIDC group claim propagation for SFTPGo which cannot read groups from JWT claims.",
      "tags": [
        "sftpgo",
        "users",
        "groups",
        "sync"
      ],
      "versions": [
        "v1.0.0"
      ],
      "latest": "v1.0.0"
    },
    {
      "id": "datadog-teams-sync",
      "name": "Datadog Teams Sync",
      "description": "Reads JustIAM app mappings (variable_name=teams) as the source of truth, creates/deletes matching Datadog teams, and keeps SAML authn_mappings in sync so team membership is propagated via SAML assertion.",
      "tags": [
        "datadog",
        "teams",
        "saml",
        "sync"
      ],
      "versions": [
        "v0.0.1",
        "v0.0.2",
        "v0.0.3"
      ],
      "latest": "v0.0.3"
    },
    {
      "id": "datadog-roles-sync",
      "name": "Datadog Roles Sync",
      "description": "Reads JustIAM app mappings (variable_name=roles) as the source of truth and keeps Datadog SAML role authn_mappings in sync. Managed roles (Datadog built-ins) are never created or deleted.",
      "tags": [
        "datadog",
        "roles",
        "saml",
        "sync"
      ],
      "versions": [
        "v0.0.1",
        "v0.0.2",
        "v0.0.3"
      ],
      "latest": "v0.0.3"
    }
  ]
}
